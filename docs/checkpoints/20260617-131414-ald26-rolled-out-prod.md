---
status: in-progress
branch: horilla_aqv10
timestamp: 2026-06-17T13:14:14+0700
files_modified:
  - pwa/frontend/tsconfig.tsbuildinfo (build artifact, ignore)
---

## Working on: ALD26 đã rollout Production — monitor + dọn dẹp tùy chọn

### Summary

ALD26 (1 ca 24h chung toàn công ty, chấm nhiều lượt, span giờ vào/ra, NCO, công ngày) **đã rollout Production xong 2026-06-17** (prod 1.0 = aqv10 = `28973df61`, 361 NV). Mọi tính năng ALD26 đã sống trên cả stage + prod. Working tree sạch, không còn việc code dở. Việc còn lại là theo dõi prod + vài dọn dẹp tùy chọn + hạng mục tồn (Ronaljack/TM20).

### Decisions Made (phiên này — đã xong)
- **ALD26 = 1 ca chung** (00:00–23:58, min 9h35, check_mode both, auto-punch OFF). Tổng giờ = span lượt đầu→lượt cuối, KHÔNG trừ trưa. Công ngày = min(1.0, worked/9h35); nghỉ phép có lương = 1.0.
- Lượt chấm **phẳng** (mỗi clock_in/clock_out = 1 lượt): lượt 1 = giờ vào ca, ngày đã qua → lượt cuối = giờ ra ca, giữa = giờ chấm. 1 lượt (quá khứ) = NCO; 0 lượt = Vắng.
- Nút "Chấm công" thống nhất (bỏ vào/ra ca). Modal chi tiết gộp 1 (PunchList): badge Trong/Ngoài VP, tên VP (trong), lý do+chi tiết (ngoài), địa điểm phường/tỉnh (bỏ "cách VP X m"). Dùng ở Trang chủ + Lịch sử + CC Tháng.
- Công ngày 3 nơi: CC Tháng (cell.cong + total_cong), Home ("Ngày công"=tổng công), Payroll (_actual_cong → actual_days mặc định).
- Xuất CC Excel: lọc Công ty/Phòng ban (badge) + cột "Lượt chấm" phẳng + cột "Công".
- Rollout prod: deploy code (10 commit ff, KHÔNG migration) → `setup_ald26` (361 NV) → dọn rác 23:59 → `backfill_ald26 --month 2026-06` (62 ngày, 16 NCO). Backup: `/home/naquan/prod_predeploy_ald26_20260617_130142.sql.gz` + `prod_workinfo_shift_backup_*.json`.

### Remaining Work (ưu tiên)
1. **Theo dõi prod vài ngày**: nhân viên chấm thật → lượt chấm/giờ vào-ra/công đúng; đặc biệt người chấm >2 lượt + NCO.
2. **Kiểm tra không có punch tự động lạ** từ EmployeeShiftPlan ca cũ (chưa dọn shift plan). Nếu có → dọn EmployeeShiftPlan cũ hoặc tắt auto trên schedule ca cũ.
3. **Dọn dẹp tùy chọn (không gấp)**: tắt healthcheck sai của container `autoclock` (hết "unhealthy"); dọn EmployeeShiftPlan ca cũ.
4. **Ronaljack/TM20 Wifi** — chấm công dự phòng (ZKTeco/pyzk), CHƯA làm: chờ thông số máy (model/SDK/IP/badge_id). Xem [[horilla-ronaljack-fallback]].
5. Payroll T6: khi tạo bảng lương, actual_days tự lấy tổng công tỉ lệ (C&B sửa tay được). T5 đã xoá trước đó.

### Notes
- **Stage giờ tự đúng**: prod chạy ALD26 native → sync 12:00/20:00 mang giờ/công đúng về stage, HẾT phải backfill thủ công trên stage.
- gstack upgrade chờ: 1.58.0.0 → 1.58.1.0 (chưa nâng).
- Memory cập nhật: [[horilla-ald26-shift]] (rollout prod), MEMORY.md. Các file khác: cicd, prod-env-gotchas, attendance-cleanup, standby-stage-sync, wc2026, ronaljack-fallback.
- Lệnh hữu ích: deploy prod `git push origin origin/horilla_aqv10:1.0`; setup `manage.py setup_ald26`; backfill `manage.py backfill_ald26 --month YYYY-MM`; dọn 23:59 = UPDATE clock_out=NULL WHERE clock_out 23:50-23:59:59 AND clock_out_latitude IS NULL.
- SSH prod: `ssh -i ~/CloudSrv/es-hrm.pem naquan@100.99.164.24`; stage: `ssh -i ~/CloudSrv/naquan.pem naquan@100.88.75.106`. nginx.conf đổi → phải force-recreate.
