---
status: completed
branch: horilla_aqv10
timestamp: 2026-07-08T13:41:16+0700
files_modified:
  - leave/models.py
  - leave/scheduler.py
  - leave/migrations/0008_leaverequest_cancel_audit.py
  - leave/migrations/0009_leaverequest_approval_reminder.py
  - base/migrations/0040_appfeature_leave_approver_config.py
  - horilla_api/api_views/leave/leave_management_views.py
  - horilla_api/api_urls/leave/urls.py
  - pwa/frontend/src/pages/LeaveManagement.tsx
  - pwa/frontend/src/pages/LeaveApproverConfig.tsx
  - pwa/frontend/src/App.tsx
  - pwa/frontend/src/pages/Apps.tsx
  - docs/huong-dan-cb-nghi-phep.md
---

## Working on: C&B Leave Management — 4 đợt DEPLOY PROD xong

### Summary

Hoàn tất toàn bộ 5 yêu cầu C&B về quản lý nghỉ phép (docs/plan-cb-leave-enhancements.md),
code + deploy **PRODUCTION** thành công 08/07/2026, verify đầy đủ. Cũng đã viết
hướng dẫn nhanh cho C&B (docs/huong-dan-cb-nghi-phep.md, đã gửi user). **Không còn
việc dang dở trong scope leave management.**

### Decisions Made

- **#5 hủy đơn đã duyệt KHÔNG tự hoàn số dư** (quyết định user cũ) — C&B tự chỉnh tay qua #3.
- **#4 nhắc duyệt: 09:00 VN** (user đổi từ 08:00). Lịch: >=3 ngày (lần 1) → >=4 → >=5 rồi dừng.
- **CAS guard chống nhắc trùng**: prod 8 gunicorn worker + preload_app=True. Dùng
  atomic `LeaveRequest.objects.filter(id, reminder_count=target).update(reminder_count=target+1)`
  → chỉ 1 worker "giành" được mỗi lần nhắc. Container TZ=UTC nên CronTrigger PHẢI
  khai báo `timezone="Asia/Ho_Chi_Minh"` (APScheduler 3.11.2, string tz OK).
- **#5 dùng `lr.save()` thường** (không update_fields/.update()) để khớp
  LeaveRequestCancelAPIView chuẩn (set clashes=0 + ghi history).
- **HNHApprovedLeavesView trả TẤT CẢ đơn approved** (không chỉ conflict) + cờ
  `has_conflict`/`worked_days` → 1 endpoint phục vụ cả list lẫn cảnh báo.

### Remaining Work

Scope leave management: XONG. Không còn đợt nào.
Standing (khác scope, chưa làm):
1. Rotate MS_CLIENT_SECRET trên Azure (đã lộ chat cũ).
2. Onboarding sync Employee.is_active ↔ User.is_active (gốc lỗi login marcy).
3. (tùy chọn) Arkon embed 500 `/bff/embed/arkon/url` ECONNREFUSED :5166 (service Arkon local chết).
4. (tùy chọn) tối ưu tốc độ clock-in ~1s (reverse-geocode Nominatim).

### Notes

- **AppFeature GOTCHA (quan trọng)**: card PWA CHỈ hiện nếu slug có row active trong
  bảng DB `base.AppFeature` (nguồn của MyAppsView `/api/employee/my-apps/` `allowed`),
  KHÔNG phải mảng `features` ở Apps.tsx. Thêm feature mới = PHẢI tạo data migration
  seed AppFeature (mẫu base/migrations/00xx_appfeature_*.py, get_or_create). Superuser
  cũng bị lọc theo all_slugs (từ DB). base/0040 seed 'leave-approver-config'.
- Commits: #5=27fe56e09, #4=7de4b18da, #1=8971295be, seed+09:00=24e1fe93c, guide=(mới push).
- Endpoints mới (gate _is_cnb): hnh-cancel-approved/<pk>/, hnh-approved-leaves/,
  hnh-cb-managers/ (GET+POST), hnh-cb-managers/<pk>/ (DELETE), hnh-approver-map/.
- FE: LeaveManagement tab "Đơn nghỉ" (#5); LeaveApproverConfig /leave/approver-config (#1);
  LeaveOverview modal chi tiết + sửa số dư (#2/#3, đợt trước).
- Migration prod đã chạy: leave 0008, leave 0009, base 0040.
- Deploy này chỉ đụng web+pwa (KHÔNG bff) → không ai phải re-login (thành quả Redis persist).
- Verify prod: fields OK, AppFeature active, slug in allowed=True, cb-managers 200/1 rule,
  approved-leaves 200/12 đơn, no-auth 401, job hour=9.
- Deploy prod: ssh -i ~/CloudSrv/es-hrm.pem naquan@100.99.164.24, /opt/hnh/horilla,
  docker-compose.prod.yml. Gotcha container name conflict → `docker rm -f $(docker ps -aq --filter name=horilla-web-1)` trước khi up.
